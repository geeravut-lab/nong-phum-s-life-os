# Life OS — Feature Map & Backlog

**เอกสารนี้คือแหล่งความจริงเดียว (single source of truth) เรื่องสถานะฟีเจอร์**

อัปเดตล่าสุด: **2026-09-26** (รอบแก้ dead code / date key / delete wiring / Google Places / D)

เอกสารอื่นที่เกี่ยวข้องและสถานะของมัน:

| เอกสาร                      | สถานะ                                                                         |
| --------------------------- | ----------------------------------------------------------------------------- |
| `PHASE-0-MIGRATION.md`      | ปิดแล้ว (เก็บเป็นประวัติ) — เหลือ DoD ข้อ 1 (เดิน checklist) กับข้อ 4         |
| `PHASE-1-PLAN.md`           | **ถูกแทนที่ด้วยเอกสารนี้เรื่องสถานะ** — อ่านเฉพาะรายละเอียดดีไซน์/สเปกต่อขั้น |
| `GROK-SESSION-KNOWLEDGE.md` | บันทึกย้อนหลังของเซสชัน Grok — **อย่าใช้อ้างอิงสถานะ** มีจุดที่ผิดแล้ว        |
| `SPEC-COVERAGE-MAP.md`      | แมปสเปก PDF ทั้ง 3 ไฟล์ ↔ เมนู/หน้าจริง — ใช้ดูว่ายังขาดอะไรจากสเปก           |
| `BILLING-DESIGN.md` ฯลฯ     | สเปกเชิงออกแบบของแต่ละโมดูล ยังใช้ได้                                         |

---

## เกณฑ์สถานะ (เปลี่ยนแล้ว — อ่านก่อนติ๊ก)

รอบที่แล้วใช้เกณฑ์ว่า "โค้ดมีแล้ว" = ✅ ซึ่งทำให้เอกสารบอกว่าเสร็จทั้งที่ผู้ใช้กดไม่ได้จริง
สาเหตุคือมีการเขียน server function แล้ว import ไว้ แต่ไม่ได้ต่อเข้าปุ่ม — โค้ดคอมไพล์ผ่าน build ผ่าน
แต่ฟีเจอร์ไม่ทำงาน **ห้ามใช้เกณฑ์นั้นอีก**

| สัญลักษณ์ | ความหมาย                                                                  |
| --------- | ------------------------------------------------------------------------- |
| ✅        | **กดใช้งานได้จริงในเบราว์เซอร์แล้ว** (มีคนทดสอบเส้นทางนั้นจริง)           |
| 🧪        | โค้ดครบและ build/typecheck/lint ผ่าน **แต่ยังไม่มีใครทดสอบในเบราว์เซอร์** |
| 🟡        | บางส่วน / MVP / ยังขาดเงื่อนไขภายนอก (เช่น API key)                       |
| ❌        | ยังไม่มี                                                                  |

**กฎ:** ห้ามเปลี่ยน 🧪 → ✅ เพราะอ่านโค้ดแล้วคิดว่าถูก ต้องกดในเบราว์เซอร์จริงก่อน

### ตัวดักไม่ให้เกิดปัญหาเดิม

`eslint.config.js` เปิด `@typescript-eslint/no-unused-vars` เป็น **error** แล้ว (เคยตั้งเป็น `off`)
กฎนี้คือตัวที่จับ "เขียนฟังก์ชันแล้วลืมต่อปุ่ม" — ตอนเปิดกลับมาเจอของตายทันที 10 จุด **ห้ามปิดกฎนี้อีก**
ถ้าต้องมีตัวแปรที่ไม่ได้ใช้จริง ๆ ให้ตั้งชื่อขึ้นต้นด้วย `_`

---

## เมนู / Route

| เมนู                   | Route                                                            | ระบบ             |
| ---------------------- | ---------------------------------------------------------------- | ---------------- |
| วันนี้                 | `/today`                                                         | AI Life Manager  |
| คุยกับน้องภูมิ         | `/chat`                                                          | AI               |
| เอกสาร                 | `/docs`                                                          | Archive          |
| เรื่องที่ต้องทำ        | `/tasks`                                                         | Tasks            |
| ปฏิทินรวม              | `/agenda`                                                        | Unified Agenda   |
| รายรับ-รายจ่าย         | `/money`                                                         | Money            |
| ครอบครัว               | `/family`                                                        | Family Radar     |
| ช่วยฉันที              | `/helpme`                                                        | Marketplace      |
| แดชบอร์ดผู้ช่วย        | `/helper-dashboard`                                              | Marketplace      |
| สิทธิฉัน               | `/benefits`                                                      | Benefits         |
| ช่วยตัดสินใจ           | `/decide`                                                        | Decision         |
| ของดีใกล้บ้าน          | `/local`                                                         | Local            |
| แดชบอร์ดร้านค้า        | `/local/merchant`                                                | Local            |
| ฝากไว้ / หลังเหตุการณ์ | `/legacy` `/legacy/after`                                        | Life Legacy      |
| Memorial               | `/memorial/$token`                                               | Legacy           |
| สนับสนุน               | `/support`                                                       | Billing + Donate |
| ตั้งค่า                | `/settings`                                                      | Privacy          |
| ผู้ดูแลระบบ            | `/admin` (+ premium / payments / support / safety / marketplace) | Admin            |
| ติดตั้งเป็นแอป         | PWA (sidebar)                                                    | Platform         |

ลบออกแล้วรอบนี้: `/Old-chat` (หน้าแชทเก่าที่ลืมลบ แต่ลงทะเบียน route จริง เปิดได้) และ `/mic-test`

---

## Feature map

### AI / Docs / Tasks / Money / Agenda

| Feature                                | สถานะ | หมายเหตุ                                                     |
| -------------------------------------- | ----- | ------------------------------------------------------------ |
| Document AI + import → Legacy          | ✅    |                                                              |
| เรื่องที่ต้องทำ (สร้าง/ปิด/แชร์)       | ✅    |                                                              |
| **Recurring reminder engine**          | ✅    | `cron.server.ts` + `netlify/functions/tick.mts` ทุก 5 นาที   |
| ปฏิทินรวม (รายการ/วัน/สัปดาห์/เดือน)   | ✅    |                                                              |
| จุดวันที่ใช้ timezone ผู้ใช้           | 🧪    | เดิมคลาด 1 วัน (นัดตั้งแต่ 07:00 เด้งไปช่องวันถัดไป) แก้แล้ว |
| ลบรายการจากปฏิทินรวม                   | 🧪    | เพิ่งต่อปุ่ม + ปิดช่องโหว่สิทธิ์                             |
| แสดงชื่อผู้รับมอบหมาย (ปฏิทิน + tasks) | 🧪    |                                                              |
| สวิตช์แชร์ครอบครัวมี label             | 🧪    | เดิมมีแต่ `aria-label` มองไม่เห็นว่าปุ่มทำอะไร               |

### Marketplace (ช่วยฉันที)

| Feature                           | สถานะ | หมายเหตุ                                             |
| --------------------------------- | ----- | ---------------------------------------------------- |
| Escrow / PromptPay / Admin payout | ✅    |                                                      |
| Chat + evidence + rating          | ✅    |                                                      |
| Safety report                     | ✅    |                                                      |
| Match score เต็มสเปก              | 🟡    |                                                      |
| **ถอนข้อเสนอ (withdraw offer)**   | ❌    | โค้ดเคยมีครบแต่ไม่มีปุ่ม ถูกลบออกรอบนี้ — ดู Backlog |

### Local (ของดีใกล้บ้าน)

| Feature                       | สถานะ | หมายเหตุ                                                   |
| ----------------------------- | ----- | ---------------------------------------------------------- |
| Google Places (ค้นหา+ใกล้ฉัน) | 🟡    | แก้ 3 จุดบนหน้าเว็บแล้ว **ยังไม่ยืนยันฝั่ง Google** ดูล่าง |
| maps_url บนการ์ดสถานที่       | ✅    |                                                            |
| โปรโมชัน แก้ไข/ลบ             | ✅    |                                                            |
| สถานที่ของฉัน แก้ไข           | ✅    |                                                            |
| สถานที่ของฉัน ลบ              | 🧪    | เพิ่งเพิ่ม (โปรโมชันลบตามด้วย CASCADE)                     |

### Family / Agenda

| Feature                | สถานะ | หมายเหตุ                                    |
| ---------------------- | ----- | ------------------------------------------- |
| นัดหมาย / มอบหมายงาน   | ✅    |                                             |
| ลบนัดในหน้าครอบครัว    | 🧪    | เพิ่งต่อปุ่ม (ผู้สร้างหรือเจ้าของครอบครัว)  |
| แก้ไขนัดในหน้าครอบครัว | 🧪    | ใช้ `updateAgendaItem` ตัวเดียวกับปฏิทินรวม |
| จุดแดงเมนูปฏิทินรวม    | 🧪    | เขียน notification ที่ href `/agenda`       |

### Life Legacy

| Feature                            | สถานะ |
| ---------------------------------- | ----- |
| Vault / wishes / contacts / invite | ✅    |
| Death case + post-life             | ✅    |
| Memorial + digital wreath          | ✅    |

### Platform / Admin / Billing

| Feature                                          | สถานะ | หมายเหตุ                              |
| ------------------------------------------------ | ----- | ------------------------------------- |
| PWA ติดตั้งเป็นแอป                               | ✅    |                                       |
| Free / Premium / Family + PAYG                   | ✅    |                                       |
| Plan badge (sidebar) + แพ็กปัจจุบัน + วันหมดอายุ | ✅    | หน้า `/support` แสดงครบอยู่แล้ว       |
| ปุ่มแพ็กที่ใช้อยู่ disabled                      | ✅    |                                       |
| Admin ประวัติ: ชื่อ → email → 6 ตัวแรกของ UID    | 🧪    | email ต้องดึงฝั่ง server (auth.users) |
| Netlify build (`npm ci`)                         | ✅    | ยืนยันจาก production deploy จริง      |

---

## Google Places — สิ่งที่ยังต้องยืนยัน

หน้าเว็บมี 3 ข้อผิดพลาดที่ทำให้ "ตั้ง key แล้วแต่ไม่เห็นผล" และแก้แล้วทั้งหมด:

1. `search()` ไม่เคยเรียก Google เลย — เรียกแค่ตัว AI จัดอันดับของในฐานข้อมูล แล้วขึ้นข้อความ "จัดอันดับสถานที่แล้ว"
2. ข้อความ error ถูกซ่อนใน `googlePlaces.length > 0 &&` → แสดงเฉพาะตอน**มี**ผลลัพธ์ ตอนพังจึงไม่เห็นอะไร
3. `catch { /* ignore */ }` กลืน error ทิ้ง

**ยังไม่ยืนยัน** ว่า key / Places API (New) / domain restriction ฝั่ง Google ถูกต้อง — ก่อนหน้านี้วินิจฉัยไม่ได้เพราะหน้าเว็บทิ้ง error ทุกตัว
ตอนนี้ถ้ายังมีปัญหา **ข้อความจะขึ้นใต้หัวข้อ "Google Places"** ให้อ่านข้อความนั้นแล้วค่อยแก้ที่ต้นเหตุ

---

## Backlog ถัดไป

เรียงตามความสำคัญ:

1. **เดินทดสอบในเบราว์เซอร์เพื่อเปลี่ยน 🧪 → ✅** — ทุกข้อที่เป็น 🧪 ในเอกสารนี้
2. **Data integrity & deletion path (PDPA)** — ไม่มี FK ไป `auth.users` และไม่มี `ON DELETE CASCADE` ที่ไหน
   ลบ user แล้วข้อมูลค้างเป็น orphan ทั้งหมด รวมไฟล์ใน storage ดูรายละเอียดใน `PHASE-0-MIGRATION.md` หัวข้อ 6
3. **ช่องว่างเทียบสเปก PDF** — ดู `SPEC-COVERAGE-MAP.md` ที่แมปสเปกทั้ง 3 ไฟล์กับหน้าจริงทีละข้อ
   ตัวใหญ่สุดคือ **Universal Search / Ask My Life** (สเปกจัดเป็น 1 ใน 5 ฟังก์ชัน MVP) และ **Data Export**
4. **ถอนข้อเสนอ (withdraw offer)** ในช่วยฉันที — ปุ่มยังไม่มี (ฟังก์ชันเดิมถูกลบออกเพราะเป็น dead code)
   `job_offers.status = 'withdrawn'` และ dictionary key `offerWithdrawn` มีอยู่แล้ว
5. **Google Places** — ยืนยันฝั่ง Google ตามหัวข้อด้านบน
6. **Match score เต็มสเปก** ใน Marketplace (สเปก v2 กำหนดน้ำหนักไว้ชัด) + สวิตช์ Admin เลือก
   Commission / Service Fee ตามที่สเปกสั่ง
7. **Bundle size** — `utils` 404 KB + `index` 304 KB ควร code-split
8. **i18n ที่ยังฝังภาษาไทย** — เช่นข้อความ error ใน server function และ `"ผู้ใช้:"` ในหน้า admin premium

---

## Changelog 2026-09-26 (รอบนี้)

- เปิด `no-unused-vars` กลับเป็น error + ลบของตาย 10 จุดที่มันชี้
- ปฏิทิน: key วันที่ตาม local timezone (เดิม UTC → คลาด 1 วันตั้งแต่ 07:00 เป็นต้นไป)
- ต่อปุ่มลบในปฏิทินรวมและหน้าครอบครัว + **ปิดช่องโหว่** `deleteAgendaItem` ที่ลบข้ามผู้ใช้ได้
- `/local`: ให้ `search()` เรียก Google จริง, แสดง error, เลิกกลืน error
- เพิ่มลบสถานที่ (merchant), admin ประวัติใช้ ชื่อ → email → UID 6 ตัว, ใส่ label ให้สวิตช์แชร์
- D: ชื่อผู้รับมอบหมาย, จุดแดงเมนูปฏิทินรวม, แก้ไขนัดในหน้าครอบครัว
- ลบ route ค้าง `/Old-chat` และ `/mic-test`
- แก้ `package-lock.json` ที่หลุด sync ทำให้ `npm ci` และ Netlify production build พัง
